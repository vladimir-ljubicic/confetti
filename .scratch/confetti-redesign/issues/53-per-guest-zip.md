# 53 — "Преузми све од овог госта" on 11d

**What to build:** Third settings row on the admin guest page; the block has two rows
today (`admin/guests/[publicId]/guest-settings.tsx`).

**Status:** done

- [x] Row `Преузми све од овог госта · ZIP →` under Сакриј све
- [x] Per-uploader export kind + job scoping (`ExportKind` lacks it,
      `export-jobs.ts:23`); shares plumbing with issue 52

Refs: ALIGN §2 11d; README 9d.
